PORT=8080
LOG_LEVEL=info

DB_HOST=localhost
DB_PORT=5432
DB_NAME=template_service
DB_USER=postgres
DB_PASSWORD=

# disable is a local-development-only value. Anything that talks to a database
# over a network must use require or stronger.
DB_SSLMODE=disable
